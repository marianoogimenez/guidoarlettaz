---
const mediosDePago = [
  { name: "Efectivo", icon: "cash" },
  { name: "Tarjetas de crédito y débito", icon: "card" },
  { name: "Transferencia bancaria", icon: "bank" },
  { name: "Mercado Pago", icon: "mercadopago" },
  { name: "Planes de financiación", icon: "calendar" },
];
---

<section class="py-20 lg:py-28 bg-mint">
  <div class="max-w-7xl mx-auto px-4 sm:px-6">
    <div class="text-center max-w-2xl mx-auto mb-16">
      <p class="text-teal-primary font-semibold text-sm uppercase tracking-wider mb-3">Formas de Pago</p>
      <h2 class="font-serif text-3xl sm:text-4xl lg:text-5xl font-bold text-slate-900 mb-4">
        Pagá como te quede más cómodo
      </h2>
      <p class="text-slate-500 text-lg">
        Consultorio de atención exclusivamente privada. Varias opciones de pago para que accedas al tratamiento que necesitás.
      </p>
    </div>
    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6 max-w-4xl mx-auto">
      {mediosDePago.map((m) => (
        <div class="bg-white rounded-2xl p-6 shadow-sm hover:shadow-md transition-shadow flex items-center gap-4">
          <div class="w-12 h-12 rounded-xl bg-teal-primary/10 flex items-center justify-center text-teal-primary shrink-0">
            {m.icon === "cash" && (
              <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M17 9V7a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2m2 4h10a2 2 0 002-2v-6a2 2 0 00-2-2H9a2 2 0 00-2 2v6a2 2 0 002 2zm7-5a2 2 0 11-4 0 2 2 0 014 0z" />
              </svg>
            )}
            {m.icon === "card" && (
              <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z" />
              </svg>
            )}
            {m.icon === "bank" && (
              <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4" />
              </svg>
            )}
            {m.icon === "mercadopago" && (
              <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z" />
              </svg>
            )}
            {m.icon === "calendar" && (
              <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
              </svg>
            )}
          </div>
          <span class="font-medium text-slate-700">{m.name}</span>
        </div>
      ))}
    </div>
    <div class="mt-12 max-w-2xl mx-auto">
      <div class="bg-teal-primary rounded-2xl p-8 text-white text-center">
        <h3 class="text-xl font-bold mb-3">Primera Consulta</h3>
        <p class="text-teal-100 mb-2">Incluye evaluación completa y radiografías digitales.</p>
        <p class="text-teal-100 text-sm mb-4">Presupuesto sin cargo</p>
        <a
          href="https://wa.me/5491155551234?text=Hola%2C%20quiero%20consultar%20por%20una%20primera%20consulta"
          target="_blank"
          rel="noopener noreferrer"
          class="inline-block bg-white text-teal-primary font-semibold px-8 py-3 rounded-full hover:bg-teal-50 transition-colors"
        >
          Consultá el valor por WhatsApp
        </a>
      </div>
    </div>
  </div>
</section>
